        <div class="box post">
            <h2>修改</h2>
            <%=form_tag todo_path(@todo), :method => :put do |f| %>
                <p><%= text_field_tag(:content,nil,value: @todo.content) %></p>
                <p><%= submit_tag("提交") %></p>
            <% end %>
        </div>
